info:
  name: activate
  type: http
  seq: 1
  tags:
    - activate-controller

http:
  method: POST
  url: "{{baseUrl}}/api/v1/auth/activate"
  body:
    type: json
    data: |-
      {
        "activationToken": "",
        "password": "",
        "passwordConfirm": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/auth/activate"
      method: POST
      body:
        type: json
        data: |-
          {
            "activationToken": "",
            "password": "",
            "passwordConfirm": ""
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: |-
          {
            "success": false,
            "message": "",
            "data": {},
            "timestamp": "",
            "errors": {}
          }
