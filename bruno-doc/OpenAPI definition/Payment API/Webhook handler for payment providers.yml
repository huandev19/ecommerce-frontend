info:
  name: Webhook handler for payment providers
  type: http
  seq: 1
  tags:
    - Payment API

http:
  method: POST
  url: "{{baseUrl}}/api/store/payments/webhook"
  body:
    type: json
    data: "{}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/store/payments/webhook"
      method: POST
      body:
        type: json
        data: "{}"
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: |-
          {
            "success": false,
            "message": "",
            "data": "",
            "timestamp": "",
            "errors": {}
          }
