info:
  name: deleteCart
  type: http
  seq: 9
  tags:
    - cart-controller

http:
  method: DELETE
  url: "{{baseUrl}}/api/carts/:cartId"
  params:
    - name: cartId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/carts/:cartId"
      method: DELETE
      params:
        - name: cartId
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: |-
          {
            "success": false,
            "message": "",
            "data": {},
            "timestamp": "",
            "errors": {}
          }
