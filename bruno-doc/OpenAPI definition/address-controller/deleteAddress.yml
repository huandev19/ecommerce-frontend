info:
  name: deleteAddress
  type: http
  seq: 3
  tags:
    - address-controller

http:
  method: DELETE
  url: "{{baseUrl}}/api/store/customers/addresses/:addressId"
  params:
    - name: addressId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/store/customers/addresses/:addressId"
      method: DELETE
      params:
        - name: addressId
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: |-
          {
            "success": false,
            "message": "",
            "data": {},
            "timestamp": "",
            "errors": {}
          }
